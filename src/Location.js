import React from 'react'
import './Location.css'

export default function Location({ name, dimension }) {
  ;<section className="Location">
    <ul>
      <li>Name: {name}</li>
      <li>Dimension {dimension}</li>
    </ul>
  </section>
}
